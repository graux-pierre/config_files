Repository containing several configuration files : emacs urvtx zsh oh-my-zsh xmonad gdb git.

To clone and cp all config files (warning : do not check if some config files are present) :
```
git clone https://github.com/graux-pierre/config_files && ./install_script
```